import { PresentationalComponentsModule } from './presentational-components.module';

describe('PresentationalComponentsModule', () => {
  it('should work', () => {
    expect(new PresentationalComponentsModule()).toBeDefined();
  });
});
